export const Input = (props) => {
  return (
    <input
      {...props}
      className="px-7 py-3 inline-flex items-center justify-center rounded border border-solid border-inputBorder text-textMedium"
    />
  );
};
